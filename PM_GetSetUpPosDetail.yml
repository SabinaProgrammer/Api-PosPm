info:
  name: PM_GetSetUpPosDetail
  type: http
  seq: 15

http:
  method: GET
  url: "{{base_url}}/PM/GetSetUpPosDetail"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
