info:
  name: GetPcStore
  type: http
  seq: 22

http:
  method: GET
  url: "{{base_url}}/PcStore/access/SUPAMAS_S"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
