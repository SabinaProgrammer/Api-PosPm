info:
  name: GetPcStore
  type: http
  seq: 29

http:
  method: GET
  url: "{{base_url}}/PcStore/access/Kruawan_a"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
