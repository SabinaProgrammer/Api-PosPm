info:
  name: PM_GetProvince
  type: http
  seq: 22

http:
  method: GET
  url: "{{base_url}}/PM/GETPOSTALCODETH"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
