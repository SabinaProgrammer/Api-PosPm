info:
  name: PM_GETsearchAddress
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/TH/searchAddress?customerid=002330"
  params:
    - name: customerid
      value: "002330"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
