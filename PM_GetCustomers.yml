info:
  name: PM_GetCustomers
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/PM/GetCustomers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
