info:
  name: PM_GETLOCATIONTYPE
  type: http
  seq: 17

http:
  method: GET
  url: "{{base_url}}/TH/GETLOCATIONTYPE"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
