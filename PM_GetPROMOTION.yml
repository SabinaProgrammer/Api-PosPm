info:
  name: PM_GetPROMOTION
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/PM/GetPROMOTION?PROMOTIONID=PM26000054"
  params:
    - name: PROMOTIONID
      value: PM26000054
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
