info:
  name: SaveApproveDelete
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/POS/Approve/SaveApproveDelete"
  body:
    type: json
    data: |-
      [
        {
        "DOCNO": "RHX00001226070002",
        "ApproveDelete": "A",
        "UserID": "admin"
        },
        {
        "DOCNO": "RHX00001226070001",
        "ApproveDelete": "A",
        "UserID": "admin"
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
