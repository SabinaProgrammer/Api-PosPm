info:
  name: GetStockReject
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/POS/Approve/GetStockReject?customerid=000012"
  params:
    - name: customerid
      value: "000012"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
