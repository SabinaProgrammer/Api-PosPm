info:
  name: GetStockReject
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/POS/Approve/GetStockReject?customerid=0000"
  params:
    - name: customerid
      value: "0000"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
