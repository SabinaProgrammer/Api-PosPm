info:
  name: GetStockRejectDetail
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/POS/Approve/GetStockRejectDetail?docNo=RHX00001226070001"
  params:
    - name: docNo
      value: RHX00001226070001
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
