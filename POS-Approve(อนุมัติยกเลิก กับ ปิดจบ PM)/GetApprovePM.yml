info:
  name: GetApprovePM
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/POS/Approve/GetForAutoKeepingReport_SPC?N_LINE=M14"
  params:
    - name: N_LINE
      value: M14
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
