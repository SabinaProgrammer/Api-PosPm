info:
  name: GetStockReject
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/POS/Approve/GetStockReject?customerid=000024"
  params:
    - name: customerid
      value: "000024"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
