info:
  name: SAVEApprovePM
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/POS/Approve/ApproveSA"
  body:
    type: json
    data: |-
      {
        "UserID": "admin",
        "ListPM": [
          "PM25001887"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
