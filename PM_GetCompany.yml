info:
  name: PM_GetCompany
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/PM/GetCompany"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
