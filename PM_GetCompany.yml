info:
  name: PM_GetCompany
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/TH/GetCompany"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
