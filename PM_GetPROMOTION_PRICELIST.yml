info:
  name: PM_GetPROMOTION_PRICELIST
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/PM/GETPMPRICELIST"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
