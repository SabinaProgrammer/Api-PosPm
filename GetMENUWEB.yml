info:
  name: GetMENUWEB
  type: http
  seq: 27

http:
  method: GET
  url: "{{base_url}}/PM/GetMENUWEB"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
