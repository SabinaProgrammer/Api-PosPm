info:
  name: PM_GETPROMOTION_TARGET
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/PM/GETTARGET"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
