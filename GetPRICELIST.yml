info:
  name: GetPRICELIST
  type: http
  seq: 30

http:
  method: GET
  url: "{{base_url}}/PM/GetPRICELIST?PRICELISTID=PPL154"
  params:
    - name: PRICELISTID
      value: PPL154
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
