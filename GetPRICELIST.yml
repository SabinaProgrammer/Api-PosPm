info:
  name: GetPRICELIST
  type: http
  seq: 29

http:
  method: GET
  url: "{{base_url}}/PM/GetPRICELIST?PRICELISTID=001598"
  params:
    - name: PRICELISTID
      value: "001598"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
