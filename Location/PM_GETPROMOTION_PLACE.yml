info:
  name: PM_GETPROMOTION_PLACE
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/TH/GETPMLOCATION"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
