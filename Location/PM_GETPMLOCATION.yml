info:
  name: PM_GETPMLOCATION
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/PM/GETPMLOCATION"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
