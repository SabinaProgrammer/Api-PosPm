info:
  name: PM_GetSM
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/PM/GetSM?promotionId=PM17002411"
  params:
    - name: promotionId
      value: PM17002411
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
