info:
  name: GetLogin_MENUWEB_GROUP
  type: http
  seq: 28

http:
  method: POST
  url: "{{base_url}}/PM/GetLogin_MENUWEB_GROUP"
  body:
    type: json
    data: |-
      {
        "id": "Kruawan_a",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
