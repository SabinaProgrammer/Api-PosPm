info:
  name: PM_GETPROMOTION_DEFAULT_LOCATIONTYPE
  type: http
  seq: 15

http:
  method: GET
  url: "{{base_url}}/PM/GETPROMOTION_DEFAULT_LOCATIONTYPE"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
