info:
  name: CancelPM
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/PM/CancelPM"
  body:
    type: json
    data: |-
      {
        "PM": {
          "PROMOTIONID": "PM26000131",
          "UPDATEUSER": "CHAT"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
