info:
  name: PM_GETsearchAddress
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/PM/searchAddress?customerid=002330"
  params:
    - name: customerid
      value: "002330"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
