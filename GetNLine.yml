info:
  name: GetNLine
  type: http
  seq: 18

http:
  method: GET
  url: "{{base_url}}/PcStore/access/chat"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
