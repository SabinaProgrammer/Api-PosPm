info:
  name: GetNLine
  type: http
  seq: 24

http:
  method: GET
  url: "{{base_url}}/PcStore/access/WAREE_A"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
