info:
  name: GetPROMOTION_SIZE
  type: http
  seq: 30

http:
  method: GET
  url: "{{base_url}}/TH/GetPROMOTION_SIZE"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
