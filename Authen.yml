info:
  name: Authen
  type: http
  seq: 18

http:
  method: POST
  url: "{{base_url}}/Authen"
  body:
    type: json
    data: |-
      {
        "ID": "chat",
        "Password": "is007"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
