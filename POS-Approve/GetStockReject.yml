info:
  name: GetStockReject
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/POS/Approve/GetStockReject"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
