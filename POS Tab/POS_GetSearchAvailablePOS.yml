info:
  name: POS_GetSearchAvailablePOS
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/PM/GetSearchAvailablePOS?province=เชียงราย"
  params:
    - name: province
      value: เชียงราย
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
