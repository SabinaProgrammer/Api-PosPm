info:
  name: POS_GetSearchAvailablePOS
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/PM/GetSearchAvailablePOS?province=กรุงเทพมหานคร"
  params:
    - name: province
      value: กรุงเทพมหานคร
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
