info:
  name: POS_GetSender
  type: http
  seq: 16

http:
  method: GET
  url: "{{base_url}}/PM/GetSender?province=อุตรดิตถ์"
  params:
    - name: province
      value: อุตรดิตถ์
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
