info:
  name: POS_GetSender
  type: http
  seq: 16

http:
  method: GET
  url: "{{base_url}}/TH/GetSender?province=ชลบุรี"
  params:
    - name: province
      value: ชลบุรี
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
