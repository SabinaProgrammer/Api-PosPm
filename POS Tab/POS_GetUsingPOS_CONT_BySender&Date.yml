info:
  name: POS_GetUsingPOS_CONT_BySender&Date
  type: http
  seq: 4

http:
  method: GET

  params:
    - name: senderId
      value: "1"
      type: query
    - name: startDate

      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
